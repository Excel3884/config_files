#################################
#       General Settings        #
#################################
backend = "glx";
experimental-backends = true;
vsync = true;
dithered-present = true;

# Fading
fading = true;
fade-in-step = 0.03;
fade-out-step = 0.03;
fade-delta = 3;
